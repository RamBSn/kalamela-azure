# Terraform state — contains secrets, keep out of source control
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.*

# Variable files contain secrets
terraform.tfvars
*.auto.tfvars

# Crash logs
crash.log
crash.*.log
